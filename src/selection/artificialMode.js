// Marker class — the actual selection logic lives in VotingOverlay.
// SelectionPanel uses this to signal that player-voting mode is active.
export class ArtificialSelectionMode {
  // computeFitness is never called for this mode — selection is manual.
}
